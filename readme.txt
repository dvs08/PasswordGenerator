CREATE REACT APP: Package which sets up a defualt react app folder structure.
NPX: Allows us to get package without the need to download it. It's used when we want to build the project once only. Like taking it on rent.


useCallBack: React hook that lets your cache a function definition between re-renders.
Ex: We needed to call passgen() on number change and char change.

useEffect: A hook that lets your synchronize component with an external system

useRef: reference hook. Used for taking something's reference